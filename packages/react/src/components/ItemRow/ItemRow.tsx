/**
 * Tecton ItemRow.
 *
 * Use it wherever you need a structured row: dropdown menus, selectors,
 * contact lists, notifications, file browsers, and activity feeds.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {Item as BaseItemRow} from '@astryxdesign/core/Item';

type ItemRowBaseProps = ComponentProps<typeof BaseItemRow>;

/** Props for {@link ItemRow}. */
export type ItemRowProps = ItemRowBaseProps;

export function ItemRow(props: ItemRowProps) {
  return <BaseItemRow {...props} />;
}

ItemRow.displayName = 'ItemRow';
