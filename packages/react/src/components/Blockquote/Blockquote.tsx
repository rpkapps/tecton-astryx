/**
 * Tecton Blockquote.
 *
 * A quotation block with a rule on its inline-start edge and secondary text
 * color. Use to highlight quoted content, testimonials, or excerpts. The
 * rule and padding are logical, so they move to the right edge in
 * right-to-left locales.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {Blockquote as BaseBlockquote} from '@astryxdesign/core/Blockquote';

type BlockquoteBaseProps = ComponentProps<typeof BaseBlockquote>;

/** Props for {@link Blockquote}. */
export type BlockquoteProps = BlockquoteBaseProps;

export function Blockquote(props: BlockquoteProps) {
  return <BaseBlockquote {...props} />;
}

Blockquote.displayName = 'Blockquote';
