/**
 * Tecton CodeTheme.
 *
 * Applies syntax highlighting colors to CodeBlock and any code component in
 * the subtree. By default, code components use the theme-level syntax colors
 * (set via defineTheme({ syntax: ... })), which derive from the palette
 * (--color-text-accent for keywords, --color-text-green for strings, etc.).
 * CodeTheme lets you override those per-region. The system uses 14 semantic
 * tokens (keyword, string, comment, number, function, type, variable,
 * operator, constant, tag, attribute, property, punctuation, background)
 * validated against 11 community themes. Custom themes are created with
 * defineSyntaxTheme() and can use [light, dark] tuples for automatic
 * color-scheme adaptation.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {SyntaxTheme as BaseCodeTheme} from '@astryxdesign/core/theme';

type CodeThemeBaseProps = ComponentProps<typeof BaseCodeTheme>;

/** Props for {@link CodeTheme}. */
export type CodeThemeProps = CodeThemeBaseProps;

export function CodeTheme(props: CodeThemeProps) {
  return <BaseCodeTheme {...props} />;
}

CodeTheme.displayName = 'CodeTheme';
