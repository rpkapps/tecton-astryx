/**
 * Tecton VisuallyHidden.
 *
 * Renders content in the accessibility tree while hiding it visually. Use
 * for accessible names on icon-only controls, aria-live announcement
 * regions, and supplementary screen-reader context. Deliberately has no
 * styling props; the whole point is to stay invisible.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {VisuallyHidden as BaseVisuallyHidden} from '@astryxdesign/core/VisuallyHidden';

type VisuallyHiddenBaseProps = ComponentProps<typeof BaseVisuallyHidden>;

/** Props for {@link VisuallyHidden}. */
export type VisuallyHiddenProps = VisuallyHiddenBaseProps;

export function VisuallyHidden(props: VisuallyHiddenProps) {
  return <BaseVisuallyHidden {...props} />;
}

VisuallyHidden.displayName = 'VisuallyHidden';
