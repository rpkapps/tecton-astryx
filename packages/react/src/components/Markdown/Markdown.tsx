/**
 * Tecton Markdown.
 *
 * Use Markdown for user-generated content, AI responses, and documentation;
 * it handles headings, lists, tables, code blocks, and citations with
 * consistent styling.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {Markdown as BaseMarkdown} from '@astryxdesign/core/Markdown';

type MarkdownBaseProps = ComponentProps<typeof BaseMarkdown>;

/** Props for {@link Markdown}. */
export type MarkdownProps = MarkdownBaseProps;

export function Markdown(props: MarkdownProps) {
  return <BaseMarkdown {...props} />;
}

Markdown.displayName = 'Markdown';
