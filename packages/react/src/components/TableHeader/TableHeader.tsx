/**
 * Tecton TableHeader.
 *
 * <thead> wrapper for children mode. Holds the header row, whose cells are
 * TableHeaderCell. A row must sit inside a section: <table> cannot contain a
 * <tr> directly, because the HTML parser inserts an implied <tbody> when it
 * parses server-rendered markup and React does not when it renders on the
 * client, so the two trees mismatch on hydration. The data-driven data={...}
 * mode renders this element itself whenever columns are supplied; in
 * children mode it is yours to supply.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {TableHeader as BaseTableHeader} from '@astryxdesign/core/Table';

type TableHeaderBaseProps = ComponentProps<typeof BaseTableHeader>;

/** Props for {@link TableHeader}. */
export type TableHeaderProps = TableHeaderBaseProps;

export function TableHeader(props: TableHeaderProps) {
  return <BaseTableHeader {...props} />;
}

TableHeader.displayName = 'TableHeader';
