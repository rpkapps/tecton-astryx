/**
 * Tecton Skeleton.
 *
 * An animated shimmer placeholder that previews the shape of content while
 * it loads. Use it to build loading screens that match the layout of the
 * real content. For content with unknown dimensions, use Spinner instead.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {Skeleton as BaseSkeleton} from '@astryxdesign/core/Skeleton';

type SkeletonBaseProps = ComponentProps<typeof BaseSkeleton>;

/** Props for {@link Skeleton}. */
export type SkeletonProps = SkeletonBaseProps;

export function Skeleton(props: SkeletonProps) {
  return <BaseSkeleton {...props} />;
}

Skeleton.displayName = 'Skeleton';
