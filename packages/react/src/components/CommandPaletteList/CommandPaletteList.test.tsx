/**
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 * Regenerate with `pnpm --filter @tecton/react generate:wrappers`.
 */
import {describe, expect, it} from 'vitest';
import {CommandPaletteList} from './CommandPaletteList.js';

describe('CommandPaletteList', () => {
  it('is a component', () => {
    expect(typeof CommandPaletteList).toBe('function');
  });

  it('names itself for the React tree', () => {
    expect(CommandPaletteList.displayName).toBe('CommandPaletteList');
  });
});
