/**
 * Tecton ChatMessage.
 *
 * Sender context wrapper: handles avatar, name, metadata, and alignment
 * based on sender role.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {ChatMessage as BaseChatMessage} from '@astryxdesign/core/Chat';

type ChatMessageBaseProps = ComponentProps<typeof BaseChatMessage>;

/** Props for {@link ChatMessage}. */
export type ChatMessageProps = ChatMessageBaseProps;

export function ChatMessage(props: ChatMessageProps) {
  return <BaseChatMessage {...props} />;
}

ChatMessage.displayName = 'ChatMessage';
