/**
 * Tecton PowerSearch.
 *
 * PowerSearch is a structured filter bar where each token represents a
 * field, operator, and value. Use it for complex multi-dimensional filtering
 * when users need to combine multiple search criteria. For simple
 * single-field search, use a text input instead.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {PowerSearch as BasePowerSearch} from '@astryxdesign/core/PowerSearch';
import {tectonIconValue, type TectonIconRef} from '../../icons/renderIcon.js';

type PowerSearchBaseProps = ComponentProps<typeof BasePowerSearch>;

/** Props for {@link PowerSearch}. */
export interface PowerSearchProps extends Omit<
  PowerSearchBaseProps,
  'startIcon'
> {
  /**
   * Icon to display at the start of the input, before any filter tokens.
   * Forwarded to the internal Tokenizer. Accepts a semantic icon name, an
   * SVG icon component, or a ReactNode directly. Takes a Tecton glyph name
   * or an SVG component.
   */
  startIcon?: TectonIconRef | PowerSearchBaseProps['startIcon'];
}

export function PowerSearch({startIcon, ...rest}: PowerSearchProps) {
  return (
    <BasePowerSearch
      {...(rest as PowerSearchBaseProps)}
      startIcon={
        tectonIconValue(startIcon) as PowerSearchBaseProps['startIcon']
      }
    />
  );
}

PowerSearch.displayName = 'PowerSearch';
