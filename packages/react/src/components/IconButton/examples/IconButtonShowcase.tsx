import {Icon} from '../../Icon/Icon.js';
import {IconButton} from '../IconButton.js';

export function IconButtonShowcase() {
  return <IconButton label="Settings" icon={<Icon name="diamond-mark" />} />;
}
