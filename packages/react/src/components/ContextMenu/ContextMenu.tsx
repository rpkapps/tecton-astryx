/**
 * Tecton ContextMenu.
 *
 * A right-click context menu that appears at the cursor position. Use to
 * provide contextual actions for specific elements or regions without
 * cluttering the UI with visible buttons.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {ContextMenu as BaseContextMenu} from '@astryxdesign/core/ContextMenu';

type ContextMenuBaseProps = ComponentProps<typeof BaseContextMenu>;

/** Props for {@link ContextMenu}. */
export type ContextMenuProps = ContextMenuBaseProps;

export function ContextMenu(props: ContextMenuProps) {
  return <BaseContextMenu {...props} />;
}

ContextMenu.displayName = 'ContextMenu';
