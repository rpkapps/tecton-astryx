/**
 * Tecton SelectableCard.
 *
 * A card that toggles between selected and unselected states with an accent
 * border. For navigation use ClickableCard.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {SelectableCard as BaseSelectableCard} from '@astryxdesign/core/SelectableCard';

type SelectableCardBaseProps = ComponentProps<typeof BaseSelectableCard>;

/** Props for {@link SelectableCard}. */
export type SelectableCardProps = SelectableCardBaseProps;

export function SelectableCard(props: SelectableCardProps) {
  return <BaseSelectableCard {...props} />;
}

SelectableCard.displayName = 'SelectableCard';
