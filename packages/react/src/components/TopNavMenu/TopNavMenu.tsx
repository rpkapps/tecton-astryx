/**
 * Tecton TopNavMenu.
 *
 * Navigation item that displays a hover-triggered popover menu with rich
 * items containing an icon, title, and optional description.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {TopNavMenu as BaseTopNavMenu} from '@astryxdesign/core/TopNav';

type TopNavMenuBaseProps = ComponentProps<typeof BaseTopNavMenu>;

/** Props for {@link TopNavMenu}. */
export type TopNavMenuProps = TopNavMenuBaseProps;

export function TopNavMenu(props: TopNavMenuProps) {
  return <BaseTopNavMenu {...props} />;
}

TopNavMenu.displayName = 'TopNavMenu';
