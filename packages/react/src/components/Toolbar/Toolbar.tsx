/**
 * Tecton Toolbar.
 *
 * Toolbar is a horizontal bar with left, center, and right areas. Use it for
 * contextual actions within a content area (above a table, inside a card, or
 * in a panel), not as a page-level header. Set the size once on the toolbar
 * and all buttons, inputs, and tabs inside it match automatically.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {Toolbar as BaseToolbar} from '@astryxdesign/core/Toolbar';

type ToolbarBaseProps = ComponentProps<typeof BaseToolbar>;

/** Props for {@link Toolbar}. */
export type ToolbarProps = ToolbarBaseProps;

export function Toolbar(props: ToolbarProps) {
  return <BaseToolbar {...props} />;
}

Toolbar.displayName = 'Toolbar';
