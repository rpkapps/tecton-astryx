/**
 * Tecton ChatMessageMetadata.
 *
 * Composable metadata row for chat messages. Renders timestamp, footer
 * content, and delivery status in a single row. Direction reverses for user
 * sender. Renders nothing if all props are empty.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {ChatMessageMetadata as BaseChatMessageMetadata} from '@astryxdesign/core/Chat';

type ChatMessageMetadataBaseProps = ComponentProps<
  typeof BaseChatMessageMetadata
>;

/** Props for {@link ChatMessageMetadata}. */
export type ChatMessageMetadataProps = ChatMessageMetadataBaseProps;

export function ChatMessageMetadata(props: ChatMessageMetadataProps) {
  return <BaseChatMessageMetadata {...props} />;
}

ChatMessageMetadata.displayName = 'ChatMessageMetadata';
