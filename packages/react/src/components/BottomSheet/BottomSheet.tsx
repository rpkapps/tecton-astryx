/**
 * Tecton BottomSheet.
 *
 * A mobile touch surface for filters, actions, forms, and detail views that
 * should rise from the bottom of the viewport; use BottomSheetSwitcher for
 * multi-step flows.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {BottomSheet as BaseBottomSheet} from '@astryxdesign/core/BottomSheet';

type BottomSheetBaseProps = ComponentProps<typeof BaseBottomSheet>;

/** Props for {@link BottomSheet}. */
export type BottomSheetProps = BottomSheetBaseProps;

export function BottomSheet(props: BottomSheetProps) {
  return <BaseBottomSheet {...props} />;
}

BottomSheet.displayName = 'BottomSheet';
