/**
 * Tecton CodeBlock.
 *
 * CodeBlock renders syntax-highlighted code with line numbers, a copy
 * button, and optional collapsible sections. Use CodeBlock for multi-line
 * snippets like source files, terminal commands, and configuration examples.
 * Use Code for inline references to function names, variables, or CLI flags
 * within body text.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {CodeBlock as BaseCodeBlock} from '@astryxdesign/core/CodeBlock';

type CodeBlockBaseProps = ComponentProps<typeof BaseCodeBlock>;

/** Props for {@link CodeBlock}. */
export type CodeBlockProps = CodeBlockBaseProps;

export function CodeBlock(props: CodeBlockProps) {
  return <BaseCodeBlock {...props} />;
}

CodeBlock.displayName = 'CodeBlock';
