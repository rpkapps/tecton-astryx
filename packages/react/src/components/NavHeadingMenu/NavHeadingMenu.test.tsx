/**
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 * Regenerate with `pnpm --filter @tecton/react generate:wrappers`.
 */
import {describe, expect, it} from 'vitest';
import {NavHeadingMenu} from './NavHeadingMenu.js';

describe('NavHeadingMenu', () => {
  it('is a component', () => {
    expect(typeof NavHeadingMenu).toBe('function');
  });

  it('names itself for the React tree', () => {
    expect(NavHeadingMenu.displayName).toBe('NavHeadingMenu');
  });
});
