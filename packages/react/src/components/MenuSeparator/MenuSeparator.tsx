/**
 * Tecton MenuSeparator.
 *
 * A horizontal rule separating groups of rows in a compound menu. Renders
 * role="separator", so it is never a stop in the arrow-key order. The
 * data-driven equivalent is the `{type: "divider"}` entry in `items`; both
 * modes render this component, so they look and theme identically.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {DropdownMenuDivider as BaseMenuSeparator} from '@astryxdesign/core/DropdownMenu';

type MenuSeparatorBaseProps = ComponentProps<typeof BaseMenuSeparator>;

/** Props for {@link MenuSeparator}. */
export type MenuSeparatorProps = MenuSeparatorBaseProps;

export function MenuSeparator(props: MenuSeparatorProps) {
  return <BaseMenuSeparator {...props} />;
}

MenuSeparator.displayName = 'MenuSeparator';
