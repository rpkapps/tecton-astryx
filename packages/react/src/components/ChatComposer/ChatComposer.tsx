/**
 * Tecton ChatComposer.
 *
 * Layout shell for a chat composer. Arranges named slots (drawer, header,
 * input, footer, send) with page-radius container, elevation, a
 * keyboard-only editor focus ring, and concentric inner radius for child
 * elements.
 *
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 */
import type {ComponentProps} from 'react';
import {ChatComposer as BaseChatComposer} from '@astryxdesign/core/Chat';

type ChatComposerBaseProps = ComponentProps<typeof BaseChatComposer>;

/** Props for {@link ChatComposer}. */
export type ChatComposerProps = ChatComposerBaseProps;

export function ChatComposer(props: ChatComposerProps) {
  return <BaseChatComposer {...props} />;
}

ChatComposer.displayName = 'ChatComposer';
