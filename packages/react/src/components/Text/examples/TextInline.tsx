import {Text} from '../Text.js';

export function TextInline() {
  return (
    <Text variant="medium" display="block">
      Design tokens are <Text variant="smallData">themeable</Text> and shared
      across every surface.
    </Text>
  );
}
