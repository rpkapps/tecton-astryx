import {Text} from '../Text.js';

export function TextBasic() {
  return (
    <Text as="p">
      Targets a nearby accumulation drilled independently, with production
      routed back to a host facility.
    </Text>
  );
}
