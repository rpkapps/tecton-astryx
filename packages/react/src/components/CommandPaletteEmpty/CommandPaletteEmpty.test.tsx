/**
 * @generated by packages/react/scripts/generate-wrappers.mjs — do not edit.
 * Regenerate with `pnpm --filter @tecton/react generate:wrappers`.
 */
import {describe, expect, it} from 'vitest';
import {CommandPaletteEmpty} from './CommandPaletteEmpty.js';

describe('CommandPaletteEmpty', () => {
  it('is a component', () => {
    expect(typeof CommandPaletteEmpty).toBe('function');
  });

  it('names itself for the React tree', () => {
    expect(CommandPaletteEmpty.displayName).toBe('CommandPaletteEmpty');
  });
});
