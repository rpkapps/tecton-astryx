[
  {
    "section": "Text",
    "name": "Text primary",
    "description": "Default product UI text.",
    "sourceToken": "semantic.color.text.text",
    "cssVariable": "--tecton-color-text-primary",
    "tsToken": "theme.palette.tecton.text.primary",
    "dark": "#f6f5f8",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.1570 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.1570."
  },
  {
    "section": "Text",
    "name": "Text secondary",
    "description": "Secondary labels, descriptions, and met…",
    "sourceToken": "semantic.color.text.subtleText",
    "cssVariable": "--tecton-color-text-seconda…",
    "tsToken": "theme.palette.tecton.text.secondary",
    "dark": "#a7a2ac",
    "light": "#604e6e",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.680 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Text",
    "name": "Text disabled",
    "description": "Disabled text.",
    "sourceToken": "semantic.color.text.disabledText",
    "cssVariable": "--tecton-color-text-disable…",
    "tsToken": "theme.palette.tecton.text.disabled",
    "dark": "#545356",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.220 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.220. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Text",
    "name": "Text placeholder",
    "description": "Input placeholder text.",
    "sourceToken": "semantic.color.text.placeholderText",
    "cssVariable": "--tecton-color-text-placeho…",
    "tsToken": "theme.palette.tecton.text.placeholder",
    "dark": "#6a696c",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.310 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.310. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Text",
    "name": "Text inverse",
    "description": "Text on inverse or high-contrast surface…",
    "sourceToken": "semantic.color.text.inverseText",
    "cssVariable": "--tecton-color-text-inverse",
    "tsToken": "theme.palette.tecton.text.inverse",
    "dark": "#131214",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.50 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.50. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Text",
    "name": "Text subtlest",
    "description": "Lowest-emphasis readable text.",
    "sourceToken": "semantic.color.text.subtlestText",
    "cssVariable": "--tecton-color-text-subtles…",
    "tsToken": "theme.palette.tecton.text.subtlest",
    "dark": "#89848e",
    "light": "#7b668b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.460 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Surfaces",
    "name": "Background default",
    "description": "App and page background.",
    "sourceToken": "semantic.color.surface.base.background",
    "cssVariable": "--tecton-color-bg-default",
    "tsToken": "theme.palette.tecton.background.defau…",
    "dark": "#1d1c1f",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.100 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.100. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Surfaces",
    "name": "Background paper",
    "description": "Paper-like surface background.",
    "sourceToken": "semantic.color.surface.elevation 1.backgro…",
    "cssVariable": "--tecton-color-bg-paper",
    "tsToken": "theme.palette.tecton.background.paper",
    "dark": "#131214",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.50 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.50. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Surfaces",
    "name": "Background elevated",
    "description": "Cards, menus, popovers, and raised doc…",
    "sourceToken": "semantic.color.surface.elevation 1.backgro…",
    "cssVariable": "--tecton-color-bg-elevated",
    "tsToken": "theme.palette.tecton.background.eleva…",
    "dark": "#131214",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.50 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.50. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Surfaces",
    "name": "Divider medium",
    "description": "Default divider or border.",
    "sourceToken": "semantic.color.divider.fill",
    "cssVariable": "--tecton-color-divider-medi…",
    "tsToken": "theme.palette.tecton.divider.medium",
    "dark": "#57515c",
    "light": "#b2a1bb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.220 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.220); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Surfaces",
    "name": "Divider subtle",
    "description": "Low-emphasis separators.",
    "sourceToken": "semantic.color.divider.subtleFill",
    "cssVariable": "--tecton-color-divider-subt…",
    "tsToken": "theme.palette.tecton.divider.subtle",
    "dark": "#342f39",
    "light": "#e4dde7",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.120 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.120); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Surfaces",
    "name": "Divider strong",
    "description": "High-emphasis separators.",
    "sourceToken": "semantic.color.divider.strongFill",
    "cssVariable": "--tecton-color-divider-stro…",
    "tsToken": "theme.palette.tecton.divider.strong",
    "dark": "#6e6873",
    "light": "#9884a4",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.310 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.310); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Actions",
    "name": "primary Background",
    "description": "Default filled action surface.",
    "sourceToken": "semantic.color.roles.action.primary.backgr…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.main",
    "dark": "#5d4d68",
    "light": "#b89dc8",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.220 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.220); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Text",
    "description": "Default text on the action surface.",
    "sourceToken": "semantic.color.roles.action.primary.contra…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.contrast…",
    "dark": "#e5e0eb",
    "light": "#35214b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lilac.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.lilac.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Hover background",
    "description": "Mouse hover surface.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.hover",
    "dark": "#74647f",
    "light": "#a47db6",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.310 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.310); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Focus background",
    "description": "Keyboard focus surface.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.focused",
    "dark": "#74647f",
    "light": "#a47db6",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.310 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.310); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Press background",
    "description": "Mouse down or pressed surface.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.pressed",
    "dark": "#80708b",
    "light": "#976dac",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.370 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.370); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Active background",
    "description": "Selected or toggled surface.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.activated",
    "dark": "#80708b",
    "light": "#976dac",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.370 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.370); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Hover text",
    "description": "Text on hover.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.hoverText",
    "dark": "#ffffff",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white, foundational.color.MPL.Terrain-100%, foundational.color.Colorcet.fire-100%. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Focus text",
    "description": "Text on keyboard focus.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.focusedT…",
    "dark": "#ffffff",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white, foundational.color.MPL.Terrain-100%, foundational.color.Colorcet.fire-100%. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Press text",
    "description": "Text while pressed.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.pressedT…",
    "dark": "#ffffff",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white, foundational.color.MPL.Terrain-100%, foundational.color.Colorcet.fire-100%. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Active text",
    "description": "Text while selected or toggled.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.activate…",
    "dark": "#ffffff",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white, foundational.color.MPL.Terrain-100%, foundational.color.Colorcet.fire-100%. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Adornment",
    "description": "Default icon or adornment color.",
    "sourceToken": "semantic.color.roles.action.primary.adornm…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.adornment",
    "dark": "#beb1c8",
    "light": "#5c3878",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lilac.onDark.830 in the foundational palette. light derived from onLight (foundational.color.lilac.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Hover adornment",
    "description": "Icon or adornment on hover.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.hoverAdo…",
    "dark": "#ffffffb3",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white.transparent.70. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Focus adornment",
    "description": "Icon or adornment on focus.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.focusAdo…",
    "dark": "#ffffffb3",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white.transparent.70. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Press adornment",
    "description": "Icon or adornment while pressed.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.pressAdo…",
    "dark": "#ffffffcc",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white.transparent.80. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "primary Active adornment",
    "description": "Icon or adornment while selected.",
    "sourceToken": "semantic.color.roles.action.primary.states…",
    "cssVariable": "--tecton-color-action-prima…",
    "tsToken": "theme.palette.tecton.primary.activeAd…",
    "dark": "#ffffffcc",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white.transparent.80. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Background",
    "description": "Default filled action surface.",
    "sourceToken": "semantic.color.roles.action.secondary.back…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.main",
    "dark": "#3a343e",
    "light": "#d5cddb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.140 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.140); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Text",
    "description": "Default text on the action surface.",
    "sourceToken": "semantic.color.roles.action.secondary.cont…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.contra…",
    "dark": "#bab3c0",
    "light": "#563f67",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.830 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Hover background",
    "description": "Mouse hover surface.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.hover",
    "dark": "#514659",
    "light": "#beabc9",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.190 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.190); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Focus background",
    "description": "Keyboard focus surface.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.focused",
    "dark": "#514659",
    "light": "#beabc9",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.190 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.190); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Press background",
    "description": "Mouse down or pressed surface.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.pressed",
    "dark": "#5a4f62",
    "light": "#b59ec1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.220 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.220); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Active background",
    "description": "Selected or toggled surface.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.activa…",
    "dark": "#5a4f62",
    "light": "#b59ec1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.220 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.220); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Hover text",
    "description": "Text on hover.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.hoverT…",
    "dark": "#e4e0ea",
    "light": "#342346",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Focus text",
    "description": "Text on keyboard focus.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.focuse…",
    "dark": "#e4e0ea",
    "light": "#342346",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Press text",
    "description": "Text while pressed.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.presse…",
    "dark": "#efecf3",
    "light": "#291d35",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1440 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1440); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Active text",
    "description": "Text while selected or toggled.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.activa…",
    "dark": "#efebf4",
    "light": "#2b1c3a",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1440 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1440); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Adornment",
    "description": "Default icon or adornment color.",
    "sourceToken": "semantic.color.roles.action.secondary.ador…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.adornm…",
    "dark": "#9a91a2",
    "light": "#725687",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.560 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Hover adornment",
    "description": "Icon or adornment on hover.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.hoverA…",
    "dark": "#cbc4d5",
    "light": "#483260",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Focus adornment",
    "description": "Icon or adornment on focus.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.focusA…",
    "dark": "#cbc4d5",
    "light": "#483260",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Press adornment",
    "description": "Icon or adornment while pressed.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.pressA…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "secondary Active adornment",
    "description": "Icon or adornment while selected.",
    "sourceToken": "semantic.color.roles.action.secondary.stat…",
    "cssVariable": "--tecton-color-action-secon…",
    "tsToken": "theme.palette.tecton.secondary.active…",
    "dark": "#cdc4d8",
    "light": "#4a3067",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lilac.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.lilac.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Background",
    "description": "Default filled action surface.",
    "sourceToken": "semantic.color.roles.action.tertiary.backg…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.main",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Text",
    "description": "Default text on the action surface.",
    "sourceToken": "semantic.color.roles.action.tertiary.contr…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.contras…",
    "dark": "#bab3c0",
    "light": "#563f67",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.830 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Hover background",
    "description": "Mouse hover surface.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.hover",
    "dark": "#3a343e",
    "light": "#d5cddb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.140 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.140); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Focus background",
    "description": "Keyboard focus surface.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.focused",
    "dark": "#3a343e",
    "light": "#d5cddb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.140 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.140); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Press background",
    "description": "Mouse down or pressed surface.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.pressed",
    "dark": "#433d47",
    "light": "#cbc0d1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.160 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.160); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Active background",
    "description": "Selected or toggled surface.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.activat…",
    "dark": "#433d47",
    "light": "#cbc0d1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.160 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.160); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Hover text",
    "description": "Text on hover.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.hoverTe…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Focus text",
    "description": "Text on keyboard focus.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.focused…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Press text",
    "description": "Text while pressed.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.pressed…",
    "dark": "#e3e0e8",
    "light": "#332442",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Active text",
    "description": "Text while selected or toggled.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.activat…",
    "dark": "#e3e0e8",
    "light": "#332442",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Adornment",
    "description": "Default icon or adornment color.",
    "sourceToken": "semantic.color.roles.action.tertiary.adorn…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.adornme…",
    "dark": "#9a91a2",
    "light": "#725687",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.560 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Hover adornment",
    "description": "Icon or adornment on hover.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.hoverAd…",
    "dark": "#bcb2c4",
    "light": "#593c70",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.830 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Focus adornment",
    "description": "Icon or adornment on focus.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.focusAd…",
    "dark": "#bcb2c4",
    "light": "#593c70",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.830 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Press adornment",
    "description": "Icon or adornment while pressed.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.pressAd…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "tertiary Active adornment",
    "description": "Icon or adornment while selected.",
    "sourceToken": "semantic.color.roles.action.tertiary.state…",
    "cssVariable": "--tecton-color-action-terti…",
    "tsToken": "theme.palette.tecton.tertiary.activeA…",
    "dark": "#cbc4d5",
    "light": "#483260",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Background",
    "description": "Default outlined action surface.",
    "sourceToken": "semantic.color.roles.action.outline.backgr…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.bg",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Border",
    "description": "Default outlined action border.",
    "sourceToken": "semantic.color.roles.action.outline.border",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.border",
    "dark": "#5a4f62",
    "light": "#b59ec1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.220 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.220); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Actions",
    "name": "outlined Strong border",
    "description": "High-emphasis outlined border.",
    "sourceToken": "semantic.color.roles.action.outline.strong…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.strongB…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Text",
    "description": "Default outlined action text.",
    "sourceToken": "semantic.color.roles.action.outline.contra…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.contras…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Hover background",
    "description": "Outlined hover surface.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.bgHover",
    "dark": "#433d47",
    "light": "#cbc0d1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.160 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.160); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Focus background",
    "description": "Outlined focus surface.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.bgFocus…",
    "dark": "#433d47",
    "light": "#cbc0d1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.160 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.160); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Press background",
    "description": "Outlined pressed surface.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.bgPress…",
    "dark": "#4e4853",
    "light": "#bcafc4",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.190 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.190); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Active background",
    "description": "Outlined selected surface.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.bgActiv…",
    "dark": "#4e4853",
    "light": "#bcafc4",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.190 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.190); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Hover border",
    "description": "Border on hover.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.borderH…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Focus border",
    "description": "Border on focus.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.borderF…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Press border",
    "description": "Border while pressed.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.borderP…",
    "dark": "#d8d5de",
    "light": "#3b2b4b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1170 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1170); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Active border",
    "description": "Border while selected.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.borderA…",
    "dark": "#d9d5e1",
    "light": "#3d2951",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1170 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1170); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Hover text",
    "description": "Outlined text on hover.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.hoverTe…",
    "dark": "#e4e0ea",
    "light": "#342346",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Focus text",
    "description": "Outlined text on focus.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.focused…",
    "dark": "#e4e0ea",
    "light": "#342346",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Press text",
    "description": "Outlined text while pressed.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.pressed…",
    "dark": "#efecf3",
    "light": "#291d35",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1440 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1440); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Active text",
    "description": "Outlined text while selected.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.activat…",
    "dark": "#efebf4",
    "light": "#2b1c3a",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1440 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1440); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Adornment",
    "description": "Default outlined icon color.",
    "sourceToken": "semantic.color.roles.action.outline.adornm…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.adornme…",
    "dark": "#8b8293",
    "light": "#826396",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.460 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Hover adornment",
    "description": "Outlined icon on hover.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.hoverAd…",
    "dark": "#cbc4d5",
    "light": "#483260",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Focus adornment",
    "description": "Outlined icon on focus.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.focusAd…",
    "dark": "#cbc4d5",
    "light": "#483260",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Press adornment",
    "description": "Outlined icon while pressed.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.pressAd…",
    "dark": "#d8d5de",
    "light": "#3b2b4b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1170 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1170); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "outlined Active adornment",
    "description": "Outlined icon while selected.",
    "sourceToken": "semantic.color.roles.action.outline.states…",
    "cssVariable": "--tecton-color-action-outli…",
    "tsToken": "theme.palette.tecton.outlined.activeA…",
    "dark": "#d9d5e1",
    "light": "#3d2951",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.1170 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.1170); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Text",
    "description": "Default text-only action text.",
    "sourceToken": "semantic.color.roles.action.textOnly.text",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.text",
    "dark": "#9a91a2",
    "light": "#725687",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.560 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Actions",
    "name": "textOnly Focus background",
    "description": "Focus-only background for text actions.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.bgFocus…",
    "dark": "#00000080",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.50. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Hover text",
    "description": "Text-only hover text.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.hoverTe…",
    "dark": "#bcb2c4",
    "light": "#593c70",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.830 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Focus text",
    "description": "Text-only focus text.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.focusTe…",
    "dark": "#bcb2c4",
    "light": "#593c70",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.830 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Press text",
    "description": "Text-only pressed text.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.pressTe…",
    "dark": "#beb1c8",
    "light": "#5c3878",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lilac.onDark.830 in the foundational palette. light derived from onLight (foundational.color.lilac.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Active text",
    "description": "Text-only selected text.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.activeT…",
    "dark": "#beb1c8",
    "light": "#5c3878",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lilac.onDark.830 in the foundational palette. light derived from onLight (foundational.color.lilac.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Adornment",
    "description": "Default text-only icon color.",
    "sourceToken": "semantic.color.roles.action.textOnly.adorn…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.adornme…",
    "dark": "#716679",
    "light": "#9e80ae",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.310 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.310); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Hover adornment",
    "description": "Text-only icon on hover.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.hoverAd…",
    "dark": "#8e8199",
    "light": "#865fa0",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.460 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Focus adornment",
    "description": "Text-only icon on focus.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.focusAd…",
    "dark": "#8e8199",
    "light": "#865fa0",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.violet.onDark.460 in the foundational palette. light derived from onLight (foundational.color.violet.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Press adornment",
    "description": "Text-only icon while pressed.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.pressAd…",
    "dark": "#90809e",
    "light": "#8b5ba9",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lilac.onDark.460 in the foundational palette. light derived from onLight (foundational.color.lilac.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "textOnly Active adornment",
    "description": "Text-only icon while selected.",
    "sourceToken": "semantic.color.roles.action.textOnly.state…",
    "cssVariable": "--tecton-color-action-text-…",
    "tsToken": "theme.palette.tecton.textOnly.activeA…",
    "dark": "#90809e",
    "light": "#8b5ba9",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lilac.onDark.460 in the foundational palette. light derived from onLight (foundational.color.lilac.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Filled background",
    "description": "Disabled filled action surface.",
    "sourceToken": "semantic.color.roles.disabled.filled.disab…",
    "cssVariable": "--tecton-color-disabled-fil…",
    "tsToken": "theme.palette.tecton.disabled.filledBg",
    "dark": "#00000066",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.40. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Filled text",
    "description": "Disabled filled action text.",
    "sourceToken": "semantic.color.roles.disabled.filled.disab…",
    "cssVariable": "--tecton-color-disabled-fil…",
    "tsToken": "theme.palette.tecton.disabled.filledT…",
    "dark": "#545356",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.220 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.220. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Filled adornment",
    "description": "Disabled filled icon color.",
    "sourceToken": "semantic.color.roles.disabled.filled.disab…",
    "cssVariable": "--tecton-color-disabled-fil…",
    "tsToken": "theme.palette.tecton.disabled.filledA…",
    "dark": "#403f42",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.160 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.160. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Outline background",
    "description": "Disabled outlined action surface.",
    "sourceToken": "semantic.color.roles.disabled.outline.disa…",
    "cssVariable": "--tecton-color-disabled-out…",
    "tsToken": "theme.palette.tecton.disabled.outline…",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Outline border",
    "description": "Disabled outlined border.",
    "sourceToken": "semantic.color.roles.disabled.outline.disa…",
    "cssVariable": "--tecton-color-disabled-out…",
    "tsToken": "theme.palette.tecton.disabled.outline…",
    "dark": "#323134",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.130 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.130. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Outline text",
    "description": "Disabled outlined text.",
    "sourceToken": "semantic.color.roles.disabled.outline.disa…",
    "cssVariable": "--tecton-color-disabled-out…",
    "tsToken": "theme.palette.tecton.disabled.outline…",
    "dark": "#4b4a4d",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.190 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.190. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Outline adornment",
    "description": "Disabled outlined icon color.",
    "sourceToken": "semantic.color.roles.disabled.outline.disa…",
    "cssVariable": "--tecton-color-disabled-out…",
    "tsToken": "theme.palette.tecton.disabled.outline…",
    "dark": "#403f42",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.160 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.160. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Text-only text",
    "description": "Disabled text-only action text.",
    "sourceToken": "semantic.color.roles.disabled.textOnly.sta…",
    "cssVariable": "--tecton-color-disabled-tex…",
    "tsToken": "theme.palette.tecton.disabled.textOnl…",
    "dark": "#4b4a4d",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.190 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.190. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "disabled Text-only adornment",
    "description": "Disabled text-only icon color.",
    "sourceToken": "semantic.color.roles.disabled.textOnly.sta…",
    "cssVariable": "--tecton-color-disabled-tex…",
    "tsToken": "theme.palette.tecton.disabled.textOnl…",
    "dark": "#403f42",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.160 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.160. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Actions",
    "name": "Focus ring",
    "description": "Keyboard focus outline.",
    "sourceToken": "semantic.color.focusOutline",
    "cssVariable": "--tecton-color-focus-ring",
    "tsToken": "theme.palette.tecton.focusRing",
    "dark": "#ff52a8",
    "light": "#ff00aa",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.hotPink.onDark.460 (focus outline) in the foundational palette. light derived from onLight (foundational.color.hotPink.onLight.460 (focus outline)); the page itself shows no light value."
  },
  {
    "section": "Status",
    "name": "success Main",
    "description": "Primary status color.",
    "sourceToken": "semantic.color.roles.success.main",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.main",
    "dark": "#78c692",
    "light": "#095330",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.830 in the foundational palette. light derived from onLight (foundational.color.green.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "success Bright",
    "description": "High-visibility status accent.",
    "sourceToken": "semantic.color.roles.success.bright",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.bright",
    "dark": "#ace4bd",
    "light": "#063a25",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.1170 in the foundational palette. light derived from onLight (foundational.color.green.onLight.1170); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "success Muted",
    "description": "Low-emphasis status accent.",
    "sourceToken": "semantic.color.roles.success.muted",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.muted",
    "dark": "#217846",
    "light": "#10a055",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.310 in the foundational palette. light derived from onLight (foundational.color.green.onLight.310); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "success Filled background",
    "description": "Filled status surface.",
    "sourceToken": "semantic.color.roles.success.filled.backgr…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.filled.b…",
    "dark": "#4fa66f",
    "light": "#0c703e",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.560 in the foundational palette. light derived from onLight (foundational.color.green.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Filled text",
    "description": "Text on filled status surface.",
    "sourceToken": "semantic.color.roles.success.filled.contra…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.filled.c…",
    "dark": "#001607",
    "light": "#f3fef8",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.50 in the foundational palette. light derived from onLight (foundational.color.green.onLight.50); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Filled adornment",
    "description": "Icon on filled status surface.",
    "sourceToken": "semantic.color.roles.success.filled.adornm…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.filled.a…",
    "dark": "#001607",
    "light": "#f3fef8",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.50 in the foundational palette. light derived from onLight (foundational.color.green.onLight.50); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Filled hover background",
    "description": "Filled status hover surface.",
    "sourceToken": "semantic.color.roles.success.filled.states…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.filled.h…",
    "dark": "#61b67f",
    "light": "#0a6238",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.680 in the foundational palette. light derived from onLight (foundational.color.green.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Filled focus background",
    "description": "Filled status focus surface.",
    "sourceToken": "semantic.color.roles.success.filled.states…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.filled.f…",
    "dark": "#61b67f",
    "light": "#0a6238",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.680 in the foundational palette. light derived from onLight (foundational.color.green.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Filled press background",
    "description": "Filled status pressed surface.",
    "sourceToken": "semantic.color.roles.success.filled.states…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.filled.p…",
    "dark": "#78c692",
    "light": "#095330",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.830 in the foundational palette. light derived from onLight (foundational.color.green.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Filled active background",
    "description": "Filled status selected surface.",
    "sourceToken": "semantic.color.roles.success.filled.states…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.filled.a…",
    "dark": "#78c692",
    "light": "#095330",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.830 in the foundational palette. light derived from onLight (foundational.color.green.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Outline background",
    "description": "Outlined status surface.",
    "sourceToken": "semantic.color.roles.success.outline.backg…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.outline.…",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Outline strong border",
    "description": "Outlined status emphasis border.",
    "sourceToken": "semantic.color.roles.success.outline.stron…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.outline.…",
    "dark": "#78c692",
    "light": "#095330",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.830 in the foundational palette. light derived from onLight (foundational.color.green.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Outline text",
    "description": "Outlined status text.",
    "sourceToken": "semantic.color.roles.success.outline.contr…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.outline.…",
    "dark": "#92d6a8",
    "light": "#07452a",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.green.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Outline adornment",
    "description": "Outlined status icon color.",
    "sourceToken": "semantic.color.roles.success.outline.adorn…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.outline.…",
    "dark": "#78c692",
    "light": "#095330",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.830 in the foundational palette. light derived from onLight (foundational.color.green.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Outline hover border",
    "description": "Outlined status border on hover.",
    "sourceToken": "semantic.color.roles.success.outline.state…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.outline.…",
    "dark": "#61b67f",
    "light": "#0a6238",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.680 in the foundational palette. light derived from onLight (foundational.color.green.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Outline focus border",
    "description": "Outlined status border on focus.",
    "sourceToken": "semantic.color.roles.success.outline.state…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.outline.…",
    "dark": "#61b67f",
    "light": "#0a6238",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.680 in the foundational palette. light derived from onLight (foundational.color.green.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Outline press border",
    "description": "Outlined status border while pressed.",
    "sourceToken": "semantic.color.roles.success.outline.state…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.outline.…",
    "dark": "#92d6a8",
    "light": "#07452a",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.green.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "success Outline active border",
    "description": "Outlined status border while selected.",
    "sourceToken": "semantic.color.roles.success.outline.state…",
    "cssVariable": "--tecton-color-status-succe…",
    "tsToken": "theme.palette.tecton.success.outline.…",
    "dark": "#92d6a8",
    "light": "#07452a",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.green.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.green.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Main",
    "description": "Primary status color.",
    "sourceToken": "semantic.color.roles.error.main",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.main",
    "dark": "#c16e6c",
    "light": "#d22f11",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.460 in the foundational palette. light derived from onLight (foundational.color.red.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "error Bright",
    "description": "High-visibility status accent.",
    "sourceToken": "semantic.color.roles.error.bright",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.bright",
    "dark": "#e3a6a6",
    "light": "#8b1f0b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.830 in the foundational palette. light derived from onLight (foundational.color.red.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "error Muted",
    "description": "Low-emphasis status accent.",
    "sourceToken": "semantic.color.roles.error.muted",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.muted",
    "dark": "#832d28",
    "light": "#f69c8f",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.190 in the foundational palette. light derived from onLight (foundational.color.red.onLight.190); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "error Filled background",
    "description": "Filled status surface.",
    "sourceToken": "semantic.color.roles.error.filled.backgrou…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.filled.bac…",
    "dark": "#c16e6c",
    "light": "#d22f11",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.460 in the foundational palette. light derived from onLight (foundational.color.red.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Filled text",
    "description": "Text on filled status surface.",
    "sourceToken": "semantic.color.roles.error.filled.contrast…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.filled.con…",
    "dark": "#2e0000",
    "light": "#fffaf9",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.50 in the foundational palette. light derived from onLight (foundational.color.red.onLight.50); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Filled adornment",
    "description": "Icon on filled status surface.",
    "sourceToken": "semantic.color.roles.error.filled.adornment",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.filled.ado…",
    "dark": "#2e0000",
    "light": "#fffaf9",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.50 in the foundational palette. light derived from onLight (foundational.color.red.onLight.50); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Filled hover background",
    "description": "Filled status hover surface.",
    "sourceToken": "semantic.color.roles.error.filled.states.h…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.filled.hov…",
    "dark": "#d89291",
    "light": "#a3240d",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.680 in the foundational palette. light derived from onLight (foundational.color.red.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Filled focus background",
    "description": "Filled status focus surface.",
    "sourceToken": "semantic.color.roles.error.filled.states.f…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.filled.foc…",
    "dark": "#d89291",
    "light": "#a3240d",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.680 in the foundational palette. light derived from onLight (foundational.color.red.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Filled press background",
    "description": "Filled status pressed surface.",
    "sourceToken": "semantic.color.roles.error.filled.states.p…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.filled.pre…",
    "dark": "#e3a6a6",
    "light": "#8b1f0b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.830 in the foundational palette. light derived from onLight (foundational.color.red.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Filled active background",
    "description": "Filled status selected surface.",
    "sourceToken": "semantic.color.roles.error.filled.states.a…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.filled.act…",
    "dark": "#e3a6a6",
    "light": "#8b1f0b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.830 in the foundational palette. light derived from onLight (foundational.color.red.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Outline background",
    "description": "Outlined status surface.",
    "sourceToken": "semantic.color.roles.error.outline.backgro…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.outline.ba…",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Outline strong border",
    "description": "Outlined status emphasis border.",
    "sourceToken": "semantic.color.roles.error.outline.strongB…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.outline.st…",
    "dark": "#cc7f7d",
    "light": "#ba2a0f",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.560 in the foundational palette. light derived from onLight (foundational.color.red.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Outline text",
    "description": "Outlined status text.",
    "sourceToken": "semantic.color.roles.error.outline.contras…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.outline.co…",
    "dark": "#cc7f7d",
    "light": "#ba2a0f",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.560 in the foundational palette. light derived from onLight (foundational.color.red.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Outline adornment",
    "description": "Outlined status icon color.",
    "sourceToken": "semantic.color.roles.error.outline.adornme…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.outline.ad…",
    "dark": "#cc7f7d",
    "light": "#ba2a0f",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.560 in the foundational palette. light derived from onLight (foundational.color.red.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Outline hover border",
    "description": "Outlined status border on hover.",
    "sourceToken": "semantic.color.roles.error.outline.states.…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.outline.ho…",
    "dark": "#d89291",
    "light": "#a3240d",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.680 in the foundational palette. light derived from onLight (foundational.color.red.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Outline focus border",
    "description": "Outlined status border on focus.",
    "sourceToken": "semantic.color.roles.error.outline.states.…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.outline.fo…",
    "dark": "#d89291",
    "light": "#a3240d",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.680 in the foundational palette. light derived from onLight (foundational.color.red.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Outline press border",
    "description": "Outlined status border while pressed.",
    "sourceToken": "semantic.color.roles.error.outline.states.…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.outline.pr…",
    "dark": "#ecbbbc",
    "light": "#751a0a",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.red.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "error Outline active border",
    "description": "Outlined status border while selected.",
    "sourceToken": "semantic.color.roles.error.outline.states.…",
    "cssVariable": "--tecton-color-status-error…",
    "tsToken": "theme.palette.tecton.error.outline.ac…",
    "dark": "#e3a6a6",
    "light": "#8b1f0b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.red.onDark.830 in the foundational palette. light derived from onLight (foundational.color.red.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Main",
    "description": "Primary status color.",
    "sourceToken": "semantic.color.roles.warning.main",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.main",
    "dark": "#f9a308",
    "light": "#693f01",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.830 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "warning Bright",
    "description": "High-visibility status accent.",
    "sourceToken": "semantic.color.roles.warning.bright",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.bright",
    "dark": "#fddf91",
    "light": "#402400",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "warning Muted",
    "description": "Low-emphasis status accent.",
    "sourceToken": "semantic.color.roles.warning.muted",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.muted",
    "dark": "#995b04",
    "light": "#c07d00",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.310 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.310); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "warning Filled background",
    "description": "Filled status surface.",
    "sourceToken": "semantic.color.roles.warning.filled.backgr…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.filled.b…",
    "dark": "#e59306",
    "light": "#7a4a00",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.680 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Filled text",
    "description": "Text on filled status surface.",
    "sourceToken": "semantic.color.roles.warning.filled.contra…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.filled.c…",
    "dark": "#1d0f01",
    "light": "#fffbf1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.50 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.50); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Filled adornment",
    "description": "Icon on filled status surface.",
    "sourceToken": "semantic.color.roles.warning.filled.adornm…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.filled.a…",
    "dark": "#1d0f01",
    "light": "#fffbf1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.50 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.50); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Filled hover background",
    "description": "Filled status hover surface.",
    "sourceToken": "semantic.color.roles.warning.filled.states…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.filled.h…",
    "dark": "#f9a308",
    "light": "#693f01",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.830 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Filled focus background",
    "description": "Filled status focus surface.",
    "sourceToken": "semantic.color.roles.warning.filled.states…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.filled.f…",
    "dark": "#f9a308",
    "light": "#693f01",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.830 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Filled press background",
    "description": "Filled status pressed surface.",
    "sourceToken": "semantic.color.roles.warning.filled.states…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.filled.p…",
    "dark": "#fbbc3b",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 2 onDark foundational tokens (foundational.color.yellow.onDark.1000, foundational.color.yellow.onDark.core.100); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Filled active background",
    "description": "Filled status selected surface.",
    "sourceToken": "semantic.color.roles.warning.filled.states…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.filled.a…",
    "dark": "#fbbc3b",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 2 onDark foundational tokens (foundational.color.yellow.onDark.1000, foundational.color.yellow.onDark.core.100); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Outline background",
    "description": "Outlined status surface.",
    "sourceToken": "semantic.color.roles.warning.outline.backg…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.outline.…",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Outline strong border",
    "description": "Outlined status emphasis border.",
    "sourceToken": "semantic.color.roles.warning.outline.stron…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.outline.…",
    "dark": "#f9a308",
    "light": "#693f01",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.830 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Outline text",
    "description": "Outlined status text.",
    "sourceToken": "semantic.color.roles.warning.outline.contr…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.outline.…",
    "dark": "#fbbc3b",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 2 onDark foundational tokens (foundational.color.yellow.onDark.1000, foundational.color.yellow.onDark.core.100); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Outline adornment",
    "description": "Outlined status icon color.",
    "sourceToken": "semantic.color.roles.warning.outline.adorn…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.outline.…",
    "dark": "#f9a308",
    "light": "#693f01",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.830 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Outline hover border",
    "description": "Outlined status border on hover.",
    "sourceToken": "semantic.color.roles.warning.outline.state…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.outline.…",
    "dark": "#e59306",
    "light": "#7a4a00",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.680 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Outline focus border",
    "description": "Outlined status border on focus.",
    "sourceToken": "semantic.color.roles.warning.outline.state…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.outline.…",
    "dark": "#e59306",
    "light": "#7a4a00",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.yellow.onDark.680 in the foundational palette. light derived from onLight (foundational.color.yellow.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Outline press border",
    "description": "Outlined status border while pressed.",
    "sourceToken": "semantic.color.roles.warning.outline.state…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.outline.…",
    "dark": "#fbbc3b",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 2 onDark foundational tokens (foundational.color.yellow.onDark.1000, foundational.color.yellow.onDark.core.100); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "warning Outline active border",
    "description": "Outlined status border while selected.",
    "sourceToken": "semantic.color.roles.warning.outline.state…",
    "cssVariable": "--tecton-color-status-warni…",
    "tsToken": "theme.palette.tecton.warning.outline.…",
    "dark": "#fbbc3b",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 2 onDark foundational tokens (foundational.color.yellow.onDark.1000, foundational.color.yellow.onDark.core.100); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Main",
    "description": "Primary status color.",
    "sourceToken": "semantic.color.roles.info.main",
    "cssVariable": "--tecton-color-status-info",
    "tsToken": "theme.palette.tecton.info.main",
    "dark": "#8ca7de",
    "light": "#2850a1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.680 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.680); the page itself shows no light value."
  },
  {
    "section": "Status",
    "name": "info Bright",
    "description": "High-visibility status accent.",
    "sourceToken": "semantic.color.roles.info.bright",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.bright",
    "dark": "#b7c9eb",
    "light": "#1c3a75",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "info Muted",
    "description": "Low-emphasis status accent.",
    "sourceToken": "semantic.color.roles.info.muted",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.muted",
    "dark": "#3766c4",
    "light": "#638ad9",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.310 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.310); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "info Filled background",
    "description": "Filled status surface.",
    "sourceToken": "semantic.color.roles.info.filled.background",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.filled.back…",
    "dark": "#6086d2",
    "light": "#3a6acb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.460 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Filled text",
    "description": "Text on filled status surface.",
    "sourceToken": "semantic.color.roles.info.filled.contrastT…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.filled.cont…",
    "dark": "#0a1324",
    "light": "#f7f7fa",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.50 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.50); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Filled adornment",
    "description": "Icon on filled status surface.",
    "sourceToken": "semantic.color.roles.info.filled.adornment",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.filled.ador…",
    "dark": "#1d3566",
    "light": "#bed0ee",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.140 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.140); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Filled hover background",
    "description": "Filled status hover surface.",
    "sourceToken": "semantic.color.roles.info.filled.states.ho…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.filled.hove…",
    "dark": "#7495d8",
    "light": "#2f5dba",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.560 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Filled focus background",
    "description": "Filled status focus surface.",
    "sourceToken": "semantic.color.roles.info.filled.states.fo…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.filled.focu…",
    "dark": "#7495d8",
    "light": "#2f5dba",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.560 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Filled press background",
    "description": "Filled status pressed surface.",
    "sourceToken": "semantic.color.roles.info.filled.states.pr…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.filled.pres…",
    "dark": "#8ca7de",
    "light": "#2850a1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.680 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Filled active background",
    "description": "Filled status selected surface.",
    "sourceToken": "semantic.color.roles.info.filled.states.ac…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.filled.acti…",
    "dark": "#8ca7de",
    "light": "#2850a1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.680 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Outline background",
    "description": "Outlined status surface.",
    "sourceToken": "semantic.color.roles.info.outline.backgrou…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.outline.bac…",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Outline strong border",
    "description": "Outlined status emphasis border.",
    "sourceToken": "semantic.color.roles.info.outline.strongBo…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.outline.str…",
    "dark": "#a0b6e4",
    "light": "#22458c",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.830 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Outline text",
    "description": "Outlined status text.",
    "sourceToken": "semantic.color.roles.info.outline.contrast…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.outline.con…",
    "dark": "#b7c9eb",
    "light": "#1c3a75",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Outline adornment",
    "description": "Outlined status icon color.",
    "sourceToken": "semantic.color.roles.info.outline.adornment",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.outline.ado…",
    "dark": "#a0b6e4",
    "light": "#22458c",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.830 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Outline hover border",
    "description": "Outlined status border on hover.",
    "sourceToken": "semantic.color.roles.info.outline.states.h…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.outline.hov…",
    "dark": "#8ca7de",
    "light": "#2850a1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.680 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Outline focus border",
    "description": "Outlined status border on focus.",
    "sourceToken": "semantic.color.roles.info.outline.states.f…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.outline.foc…",
    "dark": "#8ca7de",
    "light": "#2850a1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.680 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Outline press border",
    "description": "Outlined status border while pressed.",
    "sourceToken": "semantic.color.roles.info.outline.states.p…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.outline.pre…",
    "dark": "#b7c9eb",
    "light": "#1c3a75",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "info Outline active border",
    "description": "Outlined status border while selected.",
    "sourceToken": "semantic.color.roles.info.outline.states.a…",
    "cssVariable": "--tecton-color-status-info-…",
    "tsToken": "theme.palette.tecton.info.outline.act…",
    "dark": "#b7c9eb",
    "light": "#1c3a75",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Main",
    "description": "Primary status color.",
    "sourceToken": "semantic.color.roles.neutral.main",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.main",
    "dark": "#959497",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.560 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.560. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "neutral Bright",
    "description": "High-visibility status accent.",
    "sourceToken": "semantic.color.roles.neutral.bright",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.bright",
    "dark": "#c8c7ca",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.1000 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.1000. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "neutral Muted",
    "description": "Low-emphasis status accent.",
    "sourceToken": "semantic.color.roles.neutral.muted",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.muted",
    "dark": "#6a696c",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.310 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.310. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Status",
    "name": "neutral Filled background",
    "description": "Filled status surface.",
    "sourceToken": "semantic.color.roles.neutral.filled.backgr…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.filled.b…",
    "dark": "#2c2b2e",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.120 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.120. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Filled text",
    "description": "Text on filled status surface.",
    "sourceToken": "semantic.color.roles.neutral.filled.contra…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.filled.c…",
    "dark": "#c8c7ca",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.1000 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.1000. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Filled adornment",
    "description": "Icon on filled status surface.",
    "sourceToken": "semantic.color.roles.neutral.filled.adornm…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.filled.a…",
    "dark": "#b6b5b8",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.830 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.830. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Filled hover background",
    "description": "Filled status hover surface.",
    "sourceToken": "semantic.color.roles.neutral.filled.states…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.filled.h…",
    "dark": "#4b4a4d",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.190 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.190. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Filled focus background",
    "description": "Filled status focus surface.",
    "sourceToken": "semantic.color.roles.neutral.filled.states…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.filled.f…",
    "dark": "#4b4a4d",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.190 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.190. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Filled press background",
    "description": "Filled status pressed surface.",
    "sourceToken": "semantic.color.roles.neutral.filled.states…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.filled.p…",
    "dark": "#545356",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.220 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.220. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Filled active background",
    "description": "Filled status selected surface.",
    "sourceToken": "semantic.color.roles.neutral.filled.states…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.filled.a…",
    "dark": "#545356",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.220 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.220. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Outline background",
    "description": "Outlined status surface.",
    "sourceToken": "semantic.color.roles.neutral.outline.backg…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.outline.…",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Outline strong border",
    "description": "Outlined status emphasis border.",
    "sourceToken": "semantic.color.roles.neutral.outline.stron…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.outline.…",
    "dark": "#b6b5b8",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.830 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.830. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Outline text",
    "description": "Outlined status text.",
    "sourceToken": "semantic.color.roles.neutral.outline.contr…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.outline.…",
    "dark": "#c8c7ca",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.1000 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.1000. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Outline adornment",
    "description": "Outlined status icon color.",
    "sourceToken": "semantic.color.roles.neutral.outline.adorn…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.outline.…",
    "dark": "#b6b5b8",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.830 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.830. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Outline hover border",
    "description": "Outlined status border on hover.",
    "sourceToken": "semantic.color.roles.neutral.outline.state…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.outline.…",
    "dark": "#a5a4a7",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.680 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.680. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Outline focus border",
    "description": "Outlined status border on focus.",
    "sourceToken": "semantic.color.roles.neutral.outline.state…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.outline.…",
    "dark": "#a5a4a7",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.680 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.680. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Outline press border",
    "description": "Outlined status border while pressed.",
    "sourceToken": "semantic.color.roles.neutral.outline.state…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.outline.…",
    "dark": "#c8c7ca",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.1000 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.1000. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Status",
    "name": "neutral Outline active border",
    "description": "Outlined status border while selected.",
    "sourceToken": "semantic.color.roles.neutral.outline.state…",
    "cssVariable": "--tecton-color-status-neutr…",
    "tsToken": "theme.palette.tecton.neutral.outline.…",
    "dark": "#c8c7ca",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.1000 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.1000. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "lemon fill",
    "description": "Accent fill color.",
    "sourceToken": "semantic.color.accent.lemon.main",
    "cssVariable": "--tecton-color-accent-lemon…",
    "tsToken": "theme.palette.tecton.accent.lemon.fill",
    "dark": "#9e9813",
    "light": "#735e01",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lemon.onDark.560 in the foundational palette. light derived from onLight (foundational.color.lemon.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Accents",
    "name": "lemon text",
    "description": "Text on accent fill.",
    "sourceToken": "semantic.color.accent.lemon.contrastText",
    "cssVariable": "--tecton-color-accent-lemon…",
    "tsToken": "theme.palette.tecton.accent.lemon.con…",
    "dark": "#d6ce1a",
    "light": "#473a01",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lemon.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.lemon.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "graphite fill",
    "description": "Accent fill color.",
    "sourceToken": "semantic.color.accent.graphite.main",
    "cssVariable": "--tecton-color-accent-graph…",
    "tsToken": "theme.palette.tecton.accent.graphite.…",
    "dark": "#98939d",
    "light": "#6d5a7d",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.560 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "graphite text",
    "description": "Text on accent fill.",
    "sourceToken": "semantic.color.accent.graphite.contrastText",
    "cssVariable": "--tecton-color-accent-graph…",
    "tsToken": "theme.palette.tecton.accent.graphite.…",
    "dark": "#cac6ce",
    "light": "#433751",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "pink fill",
    "description": "Accent fill color.",
    "sourceToken": "semantic.color.accent.pink.main",
    "cssVariable": "--tecton-color-accent-pink-…",
    "tsToken": "theme.palette.tecton.accent.pink.fill",
    "dark": "#c2867a",
    "light": "#994c4c",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.pink.onDark.560 in the foundational palette. light derived from onLight (foundational.color.pink.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Accents",
    "name": "pink text",
    "description": "Text on accent fill.",
    "sourceToken": "semantic.color.accent.pink.contrastText",
    "cssVariable": "--tecton-color-accent-pink-…",
    "tsToken": "theme.palette.tecton.accent.pink.cont…",
    "dark": "#e1c3bd",
    "light": "#5c2e2e",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.pink.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.pink.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "saffron fill",
    "description": "Accent fill color.",
    "sourceToken": "semantic.color.accent.saffron.main",
    "cssVariable": "--tecton-color-accent-saffr…",
    "tsToken": "theme.palette.tecton.accent.saffron.f…",
    "dark": "#cb8553",
    "light": "#914f20",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.saffron.onDark.560 in the foundational palette. light derived from onLight (foundational.color.saffron.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "saffron text",
    "description": "Text on accent fill.",
    "sourceToken": "semantic.color.accent.saffron.contrastText",
    "cssVariable": "--tecton-color-accent-saffr…",
    "tsToken": "theme.palette.tecton.accent.saffron.c…",
    "dark": "#e5c2a9",
    "light": "#593114",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.saffron.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.saffron.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "lime fill",
    "description": "Accent fill color.",
    "sourceToken": "semantic.color.accent.lime.main",
    "cssVariable": "--tecton-color-accent-lime-…",
    "tsToken": "theme.palette.tecton.accent.lime.fill",
    "dark": "#84a138",
    "light": "#546918",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lime.onDark.560 in the foundational palette. light derived from onLight (foundational.color.lime.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Accents",
    "name": "lime text",
    "description": "Text on accent fill.",
    "sourceToken": "semantic.color.accent.lime.contrastText",
    "cssVariable": "--tecton-color-accent-lime-…",
    "tsToken": "theme.palette.tecton.accent.lime.cont…",
    "dark": "#b0d54e",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 2 onDark foundational tokens (foundational.color.lime.onDark.1000, foundational.color.lime.onDark.core.100); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "blue fill",
    "description": "Accent fill color.",
    "sourceToken": "semantic.color.accent.blue.main",
    "cssVariable": "--tecton-color-accent-blue-…",
    "tsToken": "theme.palette.tecton.accent.blue.fill",
    "dark": "#8ca7de",
    "light": "#2850a1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.680 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Accents",
    "name": "blue text",
    "description": "Text on accent fill.",
    "sourceToken": "semantic.color.accent.blue.contrastText",
    "cssVariable": "--tecton-color-accent-blue-…",
    "tsToken": "theme.palette.tecton.accent.blue.cont…",
    "dark": "#b7c9eb",
    "light": "#1c3a75",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.blue.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.blue.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Accents",
    "name": "azure fill",
    "description": "Accent fill color.",
    "sourceToken": "semantic.color.accent.azure.main",
    "cssVariable": "--tecton-color-accent-azure…",
    "tsToken": "theme.palette.tecton.accent.azure.fill",
    "dark": "#29a6a6",
    "light": "#1b6b6b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.azure.onDark.560 in the foundational palette. light derived from onLight (foundational.color.azure.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded."
  },
  {
    "section": "Accents",
    "name": "azure text",
    "description": "Text on accent fill.",
    "sourceToken": "semantic.color.accent.azure.contrastText",
    "cssVariable": "--tecton-color-accent-azure…",
    "tsToken": "theme.palette.tecton.accent.azure.con…",
    "dark": "#68d9d9",
    "light": "#114242",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.azure.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.azure.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "table / cell / background",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.table.cell.background",
    "cssVariable": "--tecton-color-component-ta…",
    "tsToken": "theme.palette.tecton.component.table.…",
    "dark": "#1d1c1f",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.100 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.100. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "table / cell / background-alt",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.table.cell.backgroundAlt",
    "cssVariable": "--tecton-color-component-ta…",
    "tsToken": "theme.palette.tecton.component.table.…",
    "dark": "#323134",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.130 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.130. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "table / cell / states / active-back…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.table.cell.states.activ…",
    "cssVariable": "--tecton-color-component-ta…",
    "tsToken": "theme.palette.tecton.component.table.…",
    "dark": "#4e4853",
    "light": "#bcafc4",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.190 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.190); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "table / cell / states / hover-back…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.table.cell.states.hover…",
    "cssVariable": "--tecton-color-component-ta…",
    "tsToken": "theme.palette.tecton.component.table.…",
    "dark": "#3a343e",
    "light": "#d5cddb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.140 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.140); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "table / header / background",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.table.header.background",
    "cssVariable": "--tecton-color-component-ta…",
    "tsToken": "theme.palette.tecton.component.table.…",
    "dark": "#433d47",
    "light": "#cbc0d1",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.160 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.160); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "table / footer",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.table.footer",
    "cssVariable": "--tecton-color-component-ta…",
    "tsToken": "theme.palette.tecton.component.table.…",
    "dark": "#28232c",
    "light": "#ebe9ee",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.110 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.110); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "top-nav / solid-background",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.topNav.solidBackground",
    "cssVariable": "--tecton-color-component-to…",
    "tsToken": "theme.palette.tecton.component.topNav…",
    "dark": "#000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black, foundational.color.MPL.Ocean-0%, foundational.color.Colorcet.fire-0%, foundational.color.Colorcet.kb-0%, …. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "top-nav / contrast-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.topNav.contrastText",
    "cssVariable": "--tecton-color-component-to…",
    "tsToken": "theme.palette.tecton.component.topNav…",
    "dark": "#ffffff80",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.white.transparent.50. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "top-nav / adornment",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.topNav.adornment",
    "cssVariable": "--tecton-color-component-to…",
    "tsToken": "theme.palette.tecton.component.topNav…",
    "dark": "#90809e",
    "light": "#8b5ba9",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.lilac.onDark.460 in the foundational palette. light derived from onLight (foundational.color.lilac.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / background",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.background",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#28232c",
    "light": "#ebe9ee",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.110 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.110); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / contrast-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.contrastTe…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / hover-bac…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.hov…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#342f39",
    "light": "#e4dde7",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.120 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.120); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / focus-bac…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.foc…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#342f39",
    "light": "#e4dde7",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.120 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.120); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / press-bac…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.pre…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#3a343e",
    "light": "#d5cddb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.140 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.140); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / active-bac…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.act…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#3a343e",
    "light": "#d5cddb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.140 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.140); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / hover-con…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.hov…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / focus-con…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.foc…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / press-con…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.pre…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#e3e0e8",
    "light": "#332442",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / active-con…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.act…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#d8d6dc",
    "light": "#382d44",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.1170 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.1170); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / hover-ado…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.hov…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / focus-ado…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.foc…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / press-ador…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.pre…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / states / active-ado…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.states.act…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#bab3c0",
    "light": "#563f67",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.830 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / adornment",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.adornment",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#8b8293",
    "light": "#826396",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.460 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / value-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.valueText",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#f7f6f8",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 3 onDark foundational tokens (foundational.color.mauve.onDark.1570, foundational.color.graphite.onDark.1570, foundational.color.violet.onDark.1570); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / filled / placeholder-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.filled.placeholde…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#98939d",
    "light": "#6d5a7d",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.560 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / background",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.backgrou…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#00000000",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches non-ramp foundational token(s): foundational.color.shades.black.transparent.0. No onDark/onLight pair, so light is null. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / border",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.border",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#57515c",
    "light": "#b2a1bb",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.220 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.220); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / contrast-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.contrast…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#98939d",
    "light": "#6d5a7d",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.560 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / value-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.valueText",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#f7f6f8",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 3 onDark foundational tokens (foundational.color.mauve.onDark.1570, foundational.color.graphite.onDark.1570, foundational.color.violet.onDark.1570); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / placeholder-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.placehol…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#89848e",
    "light": "#7b668b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.460 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / adornment",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.adornment",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#8b8293",
    "light": "#826396",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.460 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / active-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.a…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#1e1922",
    "light": "#f6f3f8",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.100 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.100); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / hover-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.h…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / focus-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.f…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / press-c…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.p…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#e3e0e8",
    "light": "#332442",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / active-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.a…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#d8d5de",
    "light": "#3b2b4b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1170 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1170); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / hover-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.h…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / focus-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.f…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / press-a…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.p…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / active-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.a…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#bab3c0",
    "light": "#563f67",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.830 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / hover-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.h…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#a7a2ac",
    "light": "#604e6e",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.680 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / focus-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.f…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#a7a2ac",
    "light": "#604e6e",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.680 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / press-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.p…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#b8b4bc",
    "light": "#52425f",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.830 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / active-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.a…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#b8b4bc",
    "light": "#52425f",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.830 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / outlined / states / active-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.outlined.states.a…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#8b8293",
    "light": "#826396",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.460 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / contrast-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.contrast…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#98939d",
    "light": "#6d5a7d",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.560 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / value-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.valueText",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#f7f6f8",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches 3 onDark foundational tokens (foundational.color.mauve.onDark.1570, foundational.color.graphite.onDark.1570, foundational.color.violet.onDark.1570); light counterpart left null because the match is ambiguous. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / placeholder-te…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.placehol…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#89848e",
    "light": "#7b668b",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.460 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / adornment",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.adornment",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#8b8293",
    "light": "#826396",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.460 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.460); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / focus-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.f…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#1e1922",
    "light": "#f6f3f8",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.100 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.100); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / hover-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.h…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac6ce",
    "light": "#433751",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / focus-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.f…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac6ce",
    "light": "#433751",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / press-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.p…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#e3e1e7",
    "light": "#31263c",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.1300 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.1300); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / active…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.a…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#d8d6dc",
    "light": "#382d44",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.graphite.onDark.1170 in the foundational palette. light derived from onLight (foundational.color.graphite.onLight.1170); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / hover-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.h…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / focus-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.f…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#aaa1b2",
    "light": "#644a78",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.680 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.680); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / press-…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.p…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#cac5d2",
    "light": "#463458",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.1000 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.1000); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "input / text-only / states / active…",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.input.textOnly.states.a…",
    "cssVariable": "--tecton-color-component-in…",
    "tsToken": "theme.palette.tecton.component.input.…",
    "dark": "#bab3c0",
    "light": "#563f67",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.mauve.onDark.830 in the foundational palette. light derived from onLight (foundational.color.mauve.onLight.830); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Source token is truncated with an ellipsis on the page. Token name is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "avatar / fill",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.avatar.fill",
    "cssVariable": "--tecton-color-component-av…",
    "tsToken": "theme.palette.tecton.component.avatar…",
    "dark": "#c2867a",
    "light": "#994c4c",
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.pink.onDark.560 in the foundational palette. light derived from onLight (foundational.color.pink.onLight.560); the page itself shows no light value. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "avatar / contrast-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.avatar.contrastText",
    "cssVariable": "--tecton-color-component-av…",
    "tsToken": "theme.palette.tecton.component.avatar…",
    "dark": "#131214",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.50 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.50. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "avatar / disabled-fill",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.avatar.disabledFill",
    "cssVariable": "--tecton-color-component-av…",
    "tsToken": "theme.palette.tecton.component.avatar…",
    "dark": "#545356",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.220 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.220. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  },
  {
    "section": "Component Tokens",
    "name": "badge / contrast-text",
    "description": "Component-scoped color token. Prefer …",
    "sourceToken": "semantic.component.badge.contrastText",
    "cssVariable": "--tecton-color-component-ba…",
    "tsToken": "theme.palette.tecton.component.badge.…",
    "dark": "#131214",
    "light": null,
    "resolvedFromJson": null,
    "matchesJson": null,
    "notes": "Source token is a semantic path; tokens/tecton.tokens.json contains only foundational.color.*, so the cited path cannot be resolved there. Value matches foundational.color.gray.onDark.contrasts.50 in the foundational palette. No onLight counterpart at foundational.color.gray.onLight.contrasts.50. CSS variable is cut off at the right edge of the captured page; only the visible prefix is recorded. React theme path is truncated with an ellipsis on the page. Description is truncated with an ellipsis on the page."
  }
]
