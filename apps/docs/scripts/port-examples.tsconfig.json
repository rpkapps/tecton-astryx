{
  "//": [
    "TEMPORARY. Typechecks the ported examples before `@tecton/react` has been",
    "reworked to re-export `@astryxdesign/core` one for one. `paths` stands in",
    "for that package: every `@tecton/react/X` resolves to upstream's own types,",
    "which is exactly what the reworked package will publish. Delete this file",
    "and port-examples.tecton-root.d.ts once",
    "`pnpm --filter @tecton/docs typecheck` covers apps/docs/examples.",
    "",
    "`@tecton/react/theme` falls through the wildcard onto upstream's own",
    "`theme` module. The real subpath also carries Tecton's theme exports",
    "(`tectonTheme`, the tokens); no example uses them, so nothing stands in",
    "for them here.",
    "",
    "`@tecton/react/icons` resolves to the generated glyph barrel rather than",
    "src/icons/index.ts, which still re-exports the `<Icon name>` wrapper the",
    "rework drops — nothing here should depend on the part of the package that",
    "is being taken out.",
    "",
    "  pnpm exec tsc -p apps/docs/scripts/port-examples.tsconfig.json --noEmit"
  ],
  "compilerOptions": {
    "target": "ESNext",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "forceConsistentCasingInFileNames": true,
    "baseUrl": "../../..",
    "paths": {
      "@tecton/react": ["apps/docs/scripts/port-examples.tecton-root.d.ts"],
      "@tecton/react/icons": ["packages/react/src/icons/generated/index.ts"],
      "@tecton/react/locales/*": [
        "packages/react/node_modules/@astryxdesign/core/locales/*"
      ],
      "@tecton/react/*": [
        "packages/react/node_modules/@astryxdesign/core/dist/*/index.d.ts",
        "packages/react/node_modules/@astryxdesign/core/dist/*.d.ts",
        "packages/react/node_modules/@astryxdesign/core/dist/*"
      ]
    }
  },
  "include": ["../examples/**/*.tsx", "./port-examples.tecton-root.d.ts"]
}
